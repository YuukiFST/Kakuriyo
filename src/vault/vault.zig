//! Vault envelope (pure std — no native_sdk import, so `zig build
//! test-vault` stays GTK-free). The v1 format and the Session API are
//! pinned in the implementation plan; this placeholder exists so the
//! staged wiring compiles while the seam lands. Task 2 fills it.
